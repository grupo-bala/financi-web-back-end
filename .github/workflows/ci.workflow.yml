name: CI workflow

on:
  push:
    branches:
      - dev

jobs:
  lint:
    name: Run Eslint
    uses: ./.github/workflows/lint.workflow.yml
  
  test:
    name: Run tests
    uses: ./.github/workflows/test.workflow.yml